// import { APP_ROUTE } from '@src/constants/route.constants';
// import { getProducts } from '@src/handlers/product.handler';
import { Router } from 'express';

const userRouter = Router();

// userRouter.get(APP_ROUTE.ROOT, );

export { userRouter };
